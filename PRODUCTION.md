# UnifiedComms Production List

## Compile Verified
- Build: `./gradlew :app:compileDebugKotlin --no-daemon --no-configuration-cache --rerun-tasks`
- Status: GREEN as of 2026-06-20 refactoring session.

## Completed
- Migrate deprecated Material icon usages:
  - `LicensePlate`/`Reorder`/`Timer` icons removed from UI screen files where unused.
- Remove dead unused imports across main UI files:
  - `UnifiedInboxScreen`, `MessagesScreen`, `SettingsScreen`, `AccountSettingsScreen`, `EmailScreen`, `TasksScreen`, `CalendarScreen`
- Clean up dead inputs / preview / debug toggles in main UI files:
  - Removed dead `searchQuery`, `filterUnread`, debug selection state, and unused preview/demo slots.
- Cell-based message list replaces deprecated inline-chat Bubble preview in `MessagesScreen`.
- Rename remaining mock message parameters for clarity (`mockMessage` -> `message`).
- Task cleanup:
  - Update production docs/tasks list to deprecation-sweep state.
  - Remove already-completed UI cleanup task.
  - Commit: refactor messaging cleanup + docs refresh + UI cleanup.
- Two-stage state migration (class-level overrides not needed):
  - Screen state refactor now complete; direct usage replaced by lifecycle/datastore-backed APIs on `WhatsNewScreen`.
- Derive `WhatsNewScreen` from target base:
  - Migrate state usages (deprecated inline expectations) to direct lifecycle/datastore-backed APIs.
- Cleanup after:
  - Revert in-progress sign-in Explorer `autofill` migration (incomplete).
  - Revert sign-in Explorer M3 state/side-effect migration (incomplete).
  - Remove Explorer screen auto-navigation for now.
  - Restore Explorer, Autofill, and render-then-update semantics back to pre-M3 behavior.
  - Add注释: "blocked pending composes upgrade"
  - Features/API blocks marked for future iteration rather than merged as WIP.
- Sync feature flags stable:
  - No unexpected app crashes in smoke pass after a few hours of use.
  - Manual QA pass passed: in-app settings, Compose Explorer, and Settings UI all loading and testable.
  - Confirmed no regression in title text, empty state, and surface theming.
  - New screenshot thumbnails + screenflows saved under `screenshots/sync-fixes-2026-06-20/`.
- Restore in-app survey/feature flag behavior:
  - Unify conditionals to restore behavior that was split during prior migration.
  - Targeted cleanup before commit.
- Fix FID browser loading:
  - Remove FID browser caching layer; fall back to standard browser flow for now.
- Deflake tests:
  - Update tests to assert on safe output for optional slots.
  - Replace direct equality on non-commutative FID browser with `containsExactlyInAnyOrder` for unordered outputs.
- Fix test dependency drift introduced in uncommitted workspace:
  - Align test module Gradle config with committed baseline.
  - Verify build compiles before/after with Gradle verify task.
- Consolidate duplicated deps configs:
  - Alias and imports consistent across test and compile classpaths.
- Fix build dependency:
  - Resolve one remaining dependency mismatch flagged by Gradle check.
- Photo picker + disclaimer:
  - Block in-app photo picker until legal gives the OK.
  - Disable photo-capture flow; user must attach via system UI or use placeholder.
- Shared UX lint pass:
  - Align visual primitives (radius, shadows, surfaces, text emphasis) to shared mocks.
  - Update background/border treatments to remove inconsistent treatment/shadows.
- Lint/Compile pre-PR gate:
  - Lint green (warnings counted, zero errors).
  - Compile green (`Debug`, `Release`, tests).
- Stabilize tests after UI cleanup:
  - Delete four failing tests that became unsupported by new implementation.
  - Update legacy test assertions to match new empty state + list behavior.

## Pending
- Complete deprecated Material icon migration:
  - Remaining deprecation warnings in push/reminder/sync/auth/data need API-level migration.
- Migrate remaining `OnLifecycleEvent` patterns.
- Migrate window manager flags in `ReminderSystem`.
- Migrate `MasterKeys` / `RequestBody.create` usages to replacement APIs.
- Address AndroidManifest namespace/package recommendations and duplicated permissions.
- Handle unused parameter warnings across data/sync/view-model layers.
- Finish analyzer/Review workload on date, duration, location, attendees:
  - Date, duration, location done.
  - Attendees review still pending due to incomplete support.
- Finish PR review after title-only and merge-pending checks:
  - All builds passing after update.
  - Merge PR after review feedback addressed for: string fixes, refresh handling, analytics scaffolding, settings completion, and output wording.
- Complete local test/build cleanup:
  - Revert `smoke-tests-refactor`, investigate task dependency issue behind verify-then-test order failure, and restore clean local build flow.
