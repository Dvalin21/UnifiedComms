<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- ponytail: attachments are written under cacheDir/attachments. Expose the
         cache root so FileProvider can serve them to external viewers. -->
    <cache-path name="attachments" path="." />
</paths>
